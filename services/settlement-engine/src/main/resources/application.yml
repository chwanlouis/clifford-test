spring:
  application:
    name: settlement-engine
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/itms_settlement
    username: ${DB_USER:itms}
    password: ${DB_PASSWORD:itms}
  jpa:
    hibernate:
      ddl-auto: update
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: settlement-engine

server:
  port: 8087

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
